<% provide(:page_title, 'All Posts') %>
<% provide(:home_active, 'active') %>

<p id="notice"><%= notice %></p>

<div class container>
	<div class="row">
		<%= render @posts %>
	</div>
</div>
	
  <tbody>
    <% @posts.each do |post| %>
     
    <% end %>
  </tbody>
</table>

<br>
